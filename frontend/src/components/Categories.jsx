import React from 'react'
import { categories } from '../data'
const Categories = () => {
  return (
    <div>
        {categories.map(item=>(
            
        ))}
    </div>
  )
}

export default Categories